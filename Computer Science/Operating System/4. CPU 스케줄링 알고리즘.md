# CPU 스케줄링 알고리즘

## 목차
- [개요](#개요)
- [핵심 용어](#핵심-용어)
- [CPU 스케줄러](#340-cpu-스케줄러)
- [비선점형 방식](#341-비선점형-방식)
    - [FCFS](#fcfs-first-come-first-served)
    - [SJF](#sjf-shortest-job-first)
    - [우선순위](#우선순위)
- [선점형 방식](#342-선점형-방식)
    - [라운드 로빈](#라운드-로빈-rr-round-robin)
    - [SRF](#srf)
    - [다단계 큐](#다단계-큐)
- [참고자료](#참고자료)

## 개요
CPU 스케줄링 알고리즘의 종류들과 의미하는 바를 알아본다.

## 핵심 용어
- `CPU 스케줄러`: 실행 준비가 되어 있는 메모리 내의 프로세스 중에서 선택하여, 이들 중 하나에게 CPU를 할당한다.

## 3.4.0 CPU 스케줄러

- CPU 스케줄러의 역할: CPU 스케줄링 알고리즘에 따라 프로세스에서 해야 하는 일을 스레드 단위로 CPU에 할당한다.
- CPU 스케줄링 알고리즘의 목적
    - CPU 이용률은 높게
    - 주어진 시간에 많은 일을 하게
    - 준비 큐(ready queue)에 있는 프로세스는 적게
    - 응답 시간은 짧게

## 3.4.1 비선점형 방식

- 의미: 프로세스가 **스스로 CPU 소유권을 포기**하는 방식
- 강제로 프로세스를 중지하지 않는다. -> 컨텍스트 스위칭으로 인한 부하가 적다.

### FCFS (First Come, First Served)
- 의미: 가장 먼저 온 것을 가장 먼저 처리하는 알고리즘
- 길게 수행되는 프로세스 때문에 **convoy effect**가 발생하는 단점이 있다.
    - convoy effect: 준비 큐에서 오래 기다리는 현상

### SJF (Shortest Job First)
- 의미: 실행 시간이 가장 짧은 프로세스를 가장 먼저 실행하는 알고리즘
- starvation 현상이 일어난다.
    - starvation: 긴 시간을 가진 프로세스가 실행되지 않는 현상
- 평균 대기 시간이 가장 짧다.
- 실제 실행 시간을 알 수 없기 때문에, 과거 실행했던 시간을 토대로 추측해서 사용한다.

### 우선순위
- 의미: SJF 알고리즘에 오래된 작업일수록 우선순위를 높이는 방법인 aging을 통해 단점을 보완한 알고리즘

## 3.4.2 선점형 방식
- 의미: 지금 사용하고 있는 프로세스를 알고리즘에 의해 중단시켜 버리고 강제로 다른 프로세스에 CPU 소유권을 할당하는 방식

### 라운드 로빈 (RR, Round Robin)
- 의미: 각 프로세스는 동일한 할당 시간을 주고 그 시간 안에 끝나지 않으면 다시 준비 큐의 뒤로 가는 알고리즘
- 우선순위 스케줄링(priority scheduling)의 일종
- 할당 시간이 너무 크면 FCFS가 된다.
- 할당 시간이 짧으면 컨텍스트 스위칭이 잦아져 오버헤드가 발생하고 비용이 커진다.
- 일반적으로 전체 작업 시간은 길어지지만 평균 응답 시간은 짧아진다.
- 로드밸런서에서 트래픽 분산 알고리즘으로도 쓰인다.

### SRF
- 의미: SJF와 다르게 중간에 실행 시간이 더 짧은 작업이 들어오면 수행하던 프로세스를 중지하고 해당 프로세스를 수행하는 알고리즘

### 다단계 큐
- 의미: 우선순위에 따른 준비 큐를 여러 개 사용하고, 큐마다 라운드 로빈이나 FCFS 등 다른 스케줄링 알고리즘을 적용한 것
- 큐 간 프로세스 이동이 불가능하여 스케줄링 부담이 적지만 유연성이 떨어진다.

## 참고자료

- 블로그
    - [CPU 스케줄링](https://imbf.github.io/computer-science(cs)/2020/10/18/CPU-Scheduling.html)